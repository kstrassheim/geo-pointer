var mapsSettings = 
{
	//the container where the map application should be created in
	appContainerSelector : ".application",
	//The viewport selector and control panel are only available in debug mode
	debugMode : false,
	mapImagePath : "./images/",
	pinImage : "./images/point.png",
	mapRows : 10,
	mapCols : 10,
	viewWidth : 600,
	viewHeight : 500,
	cookiesEnabled : false,
	selectedViewPortCountryCode : 'US',
	pinMarkingPos: "center"
};
